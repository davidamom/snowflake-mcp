version: '3.8'

services:
  snowflake-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: snowflake-mcp-container
    restart: unless-stopped
    env_file:
      - .env
    # volumes: []
      # Uncomment and adapt if you need to mount specific volumes
      # - ./your_data:/app/data
      # - ./your_config:/app/config
    # Exposing port 8000
    ports:
      - "8000:8000"
    # networks:
    #   - snowflake-network

# networks:
#   snowflake-network:
#     driver: bridge 